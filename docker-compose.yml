version: "3"
services:
    backend:
        container_name: backend
        build: .
        # environment: 
        #     - CI=true
        stdin_open: true
        tty: true
        ports: 
            - "3000:3000"
        # command: ["npm","run","test"]


